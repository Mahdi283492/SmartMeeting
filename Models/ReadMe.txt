This folder defines the structure of the data through entity classes (models), including each table, its attributes, and the relationships between them.

MVC (Model-View-Controller) architecture, this folder represents the Model layer, which handles data access and serves as the bridge between the Controller and the View.

The AppDbContext.cs database connection and handles the creation and configuration of all tables